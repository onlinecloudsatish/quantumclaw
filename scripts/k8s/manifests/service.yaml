apiVersion: v1
kind: Service
metadata:
  name: quantumclaw
  labels:
    app: quantumclaw
spec:
  type: ClusterIP
  selector:
    app: quantumclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
