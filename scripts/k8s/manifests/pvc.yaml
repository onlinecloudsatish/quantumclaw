apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quantumclaw-home-pvc
  labels:
    app: quantumclaw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
