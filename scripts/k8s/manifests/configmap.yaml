apiVersion: v1
kind: ConfigMap
metadata:
  name: quantumclaw-config
  labels:
    app: quantumclaw
data:
  quantumclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.quantumclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "QuantumClaw Assistant",
            "workspace": "~/.quantumclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # QuantumClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
